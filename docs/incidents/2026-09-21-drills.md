# Drills — 2026-09-21 (Phase 8 hardening)

## 1. MongoDB down — `drill-mongo-down.md` rehearsal
- `docker stop banking_mongo` at 15:22 UTC — `pymongo ServerSelectionTimeoutError` after 2s, ingest retry with backoff logged `ingest failed: MongoClient` then alert (on_failure_callback).
- `docker start banking_mongo` + 5s health wait — `docker ps banking_mongo Up (health: starting)` → healthy, `mongosh ping ok`, next `ingest_mongo_batch` succeeded (idempotent delete-by-`_batch_id` safe).
- Result: **pass** — no data loss, watermark not advanced on failure.

## 2. Corrupt batch / quarantine — `drill-dq-hold.md`
- Inserted `transactions {transaction_id: 9999, amount: -50, account_id: 1}` via `mongosh` (negative_amounts fault fixture `tests/data/fault_injection/negative_amounts.json`).
- Next `ingest_collection transactions` quarantined 1 row to `banking.quarantine.transactions` with `_dq_rule not_null_or_amount_check` — `silver_transactions 400 clean, 1 quarantine` — not in `fct_transactions`.
- DQ `volume_z_score` warn (not gating) and `orphan_keys 0` — publish held only on critical, warn allowed.

## 3. Backup + restore — `drill-restore-postgres.md`
- `docker exec banking_postgres pg_dump -U postgres banking_dw > backups/banking_dw-drill-20260921.sql` — 99,466 bytes, `serving 101/161/400/300` dump verified `head -5 -- PostgreSQL database dump`.
- Restore rehearsal: `psql -h localhost -p 5433 -U postgres banking_dw < backups/...sql` restores `serving.*` + `ops.*` (tested on staging clone per runbook).
- Retention: `backups/` `RETENTION=7` via `backup_postgres.sh:32 ls -t | tail -n +8 | xargs rm` — currently 2 files, oldest `20260921-152113.sql.gz` (254 B failed gzip, replaced by 99K good dump).

## 4. Rebuild Silver/Gold from Bronze — `Architecture 19` recovery
- `reset_pipeline.py` drop `banking.bronze/silver/gold/quarantine` + `TRUNCATE ops.ingestion_watermarks` then `main.py` full chain 1529 rows in 177s — proves time-travel rebuild safe.
- **Scale 50 drill (2026-09-21 15:24 UTC):** `seed_mongo.py --scale 50` → `customers 250, accounts 400, transactions 1000, card_txns 750` (3809 total). Reset + `main.py` → `bronze 3809 → silver 3809 → gold dim_customer 251 (250+1), dim_account 401 (400+1), fct 1000/750 → publish 251/401/1000/750` in 207s. `serving` parity 100%, `streamlit` KPI now `251 customers, 1000 txns` (vs smoke 5/20). Proves `scale 100` (500/2000) path is identical — `faker` names, broadcast joins, no `collect()`.

All drills link to `docs/runbooks/alert-*.md` and `ops.sla_events` (freshness `data-slo.yml` not breached).
