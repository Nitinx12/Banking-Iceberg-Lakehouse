HDFC Banking Data Platform — What I Need From You
Branch: hdfc/bank-initial | Docs: Architecture.md + PROJECT_PLAN.md Phase 0-1
Fill this file (or drop files) and I will scaffold next phases.

================================================================
1. SAMPLE DOCUMENTS — PROVIDED ✓ (2026-09-20, tests/data/*.json + mongo banking)
   Actual collections (10): customers, accounts, transactions, branches, loans, cards, card_transactions, loan_payments, support_tickets, employees
   accounts:    95,000 rows (sample in tests/data/accounts.json)
   customers:   60,000 rows (sample in tests/data/customers.json)
   transactions: 2,000,000 rows (sample in tests/data/transactions.json)
   branches:    150 rows (sample in tests/data/branches.json)
   loans:       22,000 rows (sample in tests/data/loans.json)
   cards:       65,000 rows (sample in tests/data/cards.json)
   card_transactions: 3,000,000 rows (sample in tests/data/card_transactions.json) — LARGEST
   loan_payments: 600,000 rows (sample in tests/data/loan_payments.json)
   support_tickets: 25,000 rows (sample in tests/data/support_tickets.json)
   employees:   1,800 rows (sample in tests/data/employees.json)
   Source: mongosh banking --eval db.getCollectionNames() + countDocuments()
----------------------------------------------------------------
2. VOLUME & GROWTH (Architecture 21.1) — PROFILED
   See docs/profiling.md for full field inventory, null rates, watermark choices.
   All collections: business keys unique (dups=0), created_at present 100% (no nulls), no updated_at field.
   Watermarks: all incremental on created_at (+_id tiebreaker for 2M/3M), branches=full refresh. Overlap 10m.

----------------------------------------------------------------
3. DATABRICKS WORKSPACE TIER (ADR 3 — Architecture 6.1) — PENDING
   [ ] Community Edition (no Terraform/Jobs API — fallback to Delta for Silver/Gold)
   [ ] Trial / Paid (supports Iceberg via Unity Catalog + Jobs API)
   If trial/paid, share: DATABRICKS_HOST = https://...cloud.databricks.com

----------------------------------------------------------------
4. CLOUD PROVIDER for object storage + Terraform state (Architecture 15.2) — PENDING
   [x] Local only (MinIO — already in docker-compose core) ← default unless you say AWS
   [ ] AWS S3 — bucket warehouse: ___  tfstate: ___
   [ ] Other: ___

----------------------------------------------------------------
5. REFRESH CADENCE per collection (Architecture 21.5) — PENDING
   Default if you skip: all daily incremental, branches full refresh, Flink streaming deferred to Phase 7.
   To override, mark: [daily / hourly / streaming]
   customers:    ___
   accounts:     ___
   transactions: ___  etc.

----------------------------------------------------------------
6. MONTHLY CLOUD BUDGET ceiling (PROJECT_PLAN 11, Architecture 21.6) — PENDING
   Budget USD/month: ___  (leave blank = local dev only, minimal cloud cost)

----------------------------------------------------------------
7. ALERT ROUTING (Architecture 12.2, 13) — PENDING
   SLACK_WEBHOOK_URL: ___  (skip for now = ok)
   ALERT_EMAIL_TO:    ___

----------------------------------------------------------------
8. BUSINESS TIMEZONE & LOCALE — SET
   BUSINESS_TIMEZONE (Architecture 1.4): [x] Asia/Kolkata  ← confirmed

----------------------------------------------------------------
9. DATASET LOCATION — PROVIDED ✓
   Local Mongo banking at mongodb://localhost/ + tests/data/*.json (10 collections, see section 1)
   Format: [x] live Mongo  [x] JSON export (2 docs each)  — full counts above

----------------------------------------------------------------
10. ANYTHING ELSE — OPTIONAL
   Notes:

================================================================
STATUS: 1-2 DONE ✓ (2026-09-20) — 3-10 PENDING (need your input to start Phase 1)
================================================================
How to provide:
- Edit this file directly, or reply in chat. Keep secrets in .env only — do NOT paste tokens here.
- After you fill, run: git add file.txt && git commit -m "docs: provide HDFC info for Phase 1"

Done (Phase 0):
- Profiled 10 collections (docs/profiling.md), contracts/*.yml created, sample data tests/data/*.json added, .env + compose core ready.

Next (Phase 1 — needs your input on 3-10 above):
- Spark session factory (Iceberg JDBC catalog on postgres + S3 MinIO), Bronze DDL per contracts (Architecture 5.3 _doc/_op/_source_ts/_ingested_at/_batch_id/_doc_hash), ops.ingestion_watermarks + pipeline_runs, batch job (watermark read → overlap 10m → secondaryPreferred partitioned reads → _batch_id idempotent delete-before-write → advance watermark)
